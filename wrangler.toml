# Configuración para Cloudflare Pages
name = "astro-franmoreno"
compatibility_date = "2024-12-15"

# Configuración de build para Pages
[build]
command = "pnpm run build"
cwd = "."
watch_dir = "src"

# Variables de entorno
[vars]
NODE_VERSION = "22.22.2"
